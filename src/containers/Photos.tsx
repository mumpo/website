import React, { FC } from 'react';
import AppWindow from '../components/app/AppWindow';

const Photos: FC = () => {
  return (
    <AppWindow
      title="Photos"
    >
      Attach photo
    </AppWindow>
  )
};

export default Photos;
